export enum Roles {
    CLIENT = 'client',
    ADMIN = 'admin'
}
//todo trocar accept por accepted
export enum LoanStatus {
    REQUESTED = 'requested',
    CANCELED = 'canceled',
    ACCEPT = 'accept',
    LENT = 'lent',
    RETURNED = 'returned'
}