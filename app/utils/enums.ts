export enum Roles {
    CLIENT = 'client',
    ADMIN = 'admin'
}

export enum LoanStatus {
    REQUESTED = 'requested',
    CANCELED = 'canceled',
    ACCEPT = 'accept',
    LENT = 'lent',
    RETURNED = 'returned'
}